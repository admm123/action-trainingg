name: Example group logging command

on: [push]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      name: "Group Logging"
      run: |
        echo "::group::My Group message"
        echo "Msg 1"
        echo "Msg 2"
        echo "::endgroup::"